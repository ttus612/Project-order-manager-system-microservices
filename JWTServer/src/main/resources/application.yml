eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
    register-with-eureka:
      value: true
    fetch-registry:
      value: true
    # instance:
    #   hostname: localhost